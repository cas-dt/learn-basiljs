#includepath "~/Documents/;%USERPROFILE%Documents";
#include "basiljs/bundle/basil.js";

/*
 * Create a Raster
 * Base Unit = 1 Rasterfield
 * create Rectangle that covers a random number of fields
 * is there space left?
 * create Rectangle that covers a random number of fields
 * and so on.
 * If we hit the left border, reset x to 0.
 * What happens with Fields that don’t get covered?
 * etc.
 */

function draw() {

}

b.go();
